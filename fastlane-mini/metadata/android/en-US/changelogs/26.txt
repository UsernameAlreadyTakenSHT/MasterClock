The Licenses tab now lists what this build actually ships. It was crediting four libraries this build does not contain.
A smaller download: the chess and draughts artwork is no longer bundled here, since only the Complete build displays it.
